<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kyee.restfuldemo.Dao.db1.UserDemoDao" >
    <resultMap id="UserDemoMap" type="com.kyee.restfuldemo.Entity.db1.UserDemo" >
        <id column="userid" property="userId" jdbcType="INTEGER" />
        <result column="userName" property="userName" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        userId, userName
    </sql>

    <select id="getAll" resultMap="UserDemoMap"  >
        SELECT
        <include refid="Base_Column_List" />
        FROM USERDEMO
    </select>

    <select id="getOne" parameterType="java.lang.Integer" resultMap="UserDemoMap" >
        SELECT
        <include refid="Base_Column_List" />
        FROM USERDEMO
        WHERE userid = #{userId}
    </select>

    <insert id="insert" parameterType="com.kyee.restfuldemo.Entity.db1.UserDemo" >
        INSERT
        INTO
        userdemo(userid, userName)
        VALUES(#{userId},#{userName})
    </insert>

    <update id="update" parameterType="com.kyee.restfuldemo.Entity.db1.UserDemo" >
        update
        userdemo
        set
        userName=#{userName}
        where
        userid=#{userId}
    </update>

    <delete id="delete" parameterType="com.kyee.restfuldemo.Entity.db1.UserDemo" >
        delete
        from
        userdemo
        where
        userid=#{userId}
    </delete>

</mapper>